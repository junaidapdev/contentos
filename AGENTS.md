<!-- This file mirrors CLAUDE.md so non-Claude agents find the same instructions. -->

# Instructions for Claude Code

Before touching any code in this repository, read the following in order:

1. `/context/agents.md` — the master instruction file for all AI agents working in this repo.
2. `/context/01-project-overview.md` through `/context/06-progress-tracker.md`.
3. The active feature spec in `/feature-specs/` corresponding to the chunk you have been asked to implement.

Then, and only then, begin implementation. Do not implement anything outside the active feature spec. Update `/context/06-progress-tracker.md` and `/context/decisions.md` on completion.
