# Progress Tracker

## Current Phase
Phase 4 — Launch. All 15 chunks have shipped on the engineering side. The pre-launch audit (Chunk 14, static) is complete and PASS. Live operator gate (production deploy + smoke + cross-user RLS + rate-limit verification + manual E2E) is the only remaining step before flipping the launch flag.

## Completed Chunks
- Chunk 00 — Repository bootstrap, folder scaffolding, context authoring.
- Chunk 01 — Foundational standards: Vite + React + TypeScript (strict) SPA; ESLint flat config; the constants layer; `logger.ts`; the API response envelope; `@/` + `@shared` path aliases.
- Chunk 02 — Backend: `supabase init`; 9 timestamped migrations; RLS on every user-owned table; canonical `_shared/` folder.
- Chunk 03 — Frontend Supabase client + React Query; React Router v6 data router with `RequireAuth` + `RequireOnboarded`; email/password auth + 3-step onboarding; design system migrated to Tailwind v4 + shadcn radix-nova.
- Chunk 04 — `/content-items` feature: list + filters + new + edit + delete; lifecycle state machine via `public.update_content_item_status`; `StatusBadge` with `--color-status-*` tokens.
- Chunk 05 — Ideas + **sibling spawning** (`public.spawn_sibling_content_items`).
- Chunk 06 — Cross-post grouping (`public.spawn_cross_post_variants` + partial unique index); per-platform status grid panel. `lib/pg-error-mapping.ts` extracted.
- Chunk 07 — Repurposing chains (`public.spawn_repurposed_children` + partial unique index); tree-shaped topology. `content_items_list_view` extended to 5 per-relationship aggregates.
- Chunk 08 — Unified calendar (`/calendar`): native-Date month grid; PostgREST OR-of-AND window query; URL-state filters; chips + popovers + agenda panel.
- Chunk 09 — Real dashboard (`/dashboard`): pillar balance, cadence vs targets, pipeline status mix, top empty cells. Trailing 7/30/90 day windows via URL `?window=…`. `recharts` added (`<BarChart>` only). Content-item form patched to accept `?pillar_id`/`?platform_id`. `DashboardPlaceholder.tsx` deleted.
- Chunk 10 — Brand context files (`/brand-context`) + the **content pack export** (the AI-handoff wedge). CRUD grouped by kind; export composer assembles a versioned `v1` markdown pack; verbatim `<pre>` preview, copy/download; idea-scoped export dialog. Pure `pack-builder.ts`. `lib/clipboard.ts` + `lib/download.ts`.
- Chunk 11 — **Anthropic proxy Edge Function** (`anthropic-proxy`) — the first secret-holding backend. Authenticates the JWT, enforces per-user rate limits (20/min, 200/day via `ai_request_log`), validates the request as a discriminated-union `task` schema, dispatches to a task handler, validates the model's output before returning, logs sizes/timing/code (never bodies), wraps everything in the canonical envelope, and maps every failure to a stable code (Anthropic errors never leak). New `_shared/` modules: `logger.ts`, `anthropic-config.ts`, `anthropic-client.ts`, `rate-limit.ts`, `task-schemas.ts`, `task-handlers.ts`. New migration `ai_request_log` (RLS: self-select only; service-role inserts). First in-app AI feature: **"Suggest formats with AI"** on the sibling spawn flow — calls the proxy with the idea's brand pack, pre-fills the spec rows (slug/name → id resolution), and the user reviews + edits before the manual spawn. Opt-in by click; gated on having ≥1 brand-context file. Four AI error codes added to both error-code mirrors; `GATEWAY_TIMEOUT` (504) + `reset_seconds` meta added.
- Chunk 12 — **UI polish: states, error boundary, focus, mobile, accessibility, consistency**. Shared feedback primitives in `/frontend/src/components/feedback/` (`EmptyState`, `SkeletonList`, `SkeletonCard`, `SkeletonForm`, `ErrorState`, `NetworkErrorState`, `PageHeader`, `feedback-tokens.ts`, `messages.ts`). Global `<RouteErrorBoundary>` wraps `<Outlet />` inside `<AppShell>` so a feature exception doesn't take down the sidebar; logs via `logger.error`, offers Try-again (remount via key bump) + Go-to-dashboard. `useFocusOnRouteChange` moves focus to the new page's `<h1>` after every navigation. Sonner globally configured (`position="bottom-right"`, `expand=false`, `visibleToasts=3`, closeButton); CSS rewrites position to bottom-center on `<sm`. `lib/toast.ts` is the single import point — durations locked (success 3s, error 5s, info 3s). `lib/network-error.ts` detects transport failures across Chromium/Firefox/Safari phrasings, routes to `<NetworkErrorState>` with retry. AppShell got a mobile hamburger drawer (slide-over sheet on `<md`, scrim + X + click-link all close it; active link has left-border accent — no color-alone). Per-feature: dashboard / ideas / content-items / calendar / brand-context pages adopt the shared primitives for empty / loading / error; cadence indicator pills include ✓ ↓ ↑ glyphs (color is never the sole signal). Auth pages got semantic `<h1>` for document outline. `/context/05-ui-context.md` got two new sections: "Visual Consistency Audit Checklist" and "Feedback Primitives". `/context/chunk-12-audit-notes.md` committed as the audit record. Build delta ~+5 kB gzipped (well under the 30 kB ceiling). No new dependencies; no backend changes.
- Chunk 13 — **Deployment pipeline wired (config-only)**. `frontend/vercel.json` (config-as-code: Vite preset + SPA rewrite `(/(.*) → /index.html)`); `backend/scripts/deploy.sh` (confirmation-gated migrations → Edge Function order); `scripts/smoke-test.sh` (5 HTTP checks — frontend 200, SPA fallback 200 on `/dashboard`, Supabase REST reachable, Edge Function rejects unauthenticated with 401, anon key hits PostgREST); `scripts/README.md`; `.github/workflows/ci.yml` (typecheck + lint + build on PR/push to main, placeholder VITE_* env so the Zod env validator passes — no deploy); `/context/13-deployment-runbook.md` (full operational guide: one-time setup, per-release flow, smoke test, rollback for both sides, env matrix, secrets table, quick-reference); `.gitignore` extended (`.vercel/`); `/frontend/README.md` + `/backend/README.md` got Deployment sections; `/context/02-architecture.md` got the "Realized deployment" appendix. No application code changes. Locked decisions: Vercel (not Netlify); `main` → prod / PRs → preview, no staging; backend deploys manual via `deploy.sh`; migrations before functions; no automated rollback; forward-only migrations; prod Anthropic key provisioned separately from dev; secrets via Vercel env vars (frontend) + `supabase secrets set` (backend); `vercel.json` at `/frontend/`, not repo root. **Live deploy is an operator step**, not committed here — runbook covers the procedure.
- Chunk 14 — **Pre-launch audit (static; PASS)**. All 15 items from the original code review verified PASS or documented EXCEPTION (Item 13's REST exception for the discriminated-dispatch `anthropic-proxy` ratified). RLS verified statically against migrations: every user-owned table has RLS enabled with `auth.uid() = user_id` policy; one `security definer` function (`handle_new_user` — standard Supabase auth-trigger pattern with `set search_path = public`); all other functions and the `content_items_list_view` are `security invoker` / `security_invoker = true`. Privacy audit PASS: 54 frontend logger callsites + 4 backend; all payloads are codes/ids/counts/sizes — never PII (emails, titles, notes, bodies). `ai_request_log` stores no bodies. Edge Function errorResponse messages are static strings. Service-role key and Anthropic key absent from frontend + `dist/`. Accessibility audit PASS: keyboard reach + focus management + color-not-sole-meaning + screen-reader announcements verified across every page. Bundle unchanged from Chunk 12 (1306.33 kB / 377.21 kB gz). Documentation audit PASS: no TODOs in `/context/`, every cross-ref resolves. `/context/14-pre-launch-audit.md` committed with full evidence. Static engineering-side: launch-ready. Operator gate (production SQL, cross-user RLS on production, live rate-limit, production smoke, manual E2E, perf measurements) flagged OPERATOR_RUN_PENDING in the audit doc.

## Next Up
- **Operator step (launch):** run the OPERATOR_RUN_PENDING items in `/context/14-pre-launch-audit.md` against the deployed environment, fill in the audit file's measurements, and flip "Launch ready" to YES.

## In-Progress Chunks
None on the engineering side. **Operator action pending** for both chunk-13 (live deploy) and chunk-14 (OPERATOR_RUN_PENDING audit items: production SQL, cross-user RLS on production, live rate-limit, production smoke, manual E2E, performance measurements). See `/context/13-deployment-runbook.md` for the deploy procedure and `/context/14-pre-launch-audit.md` for the audit gate.

## Blocked Chunks
None. Chunk-14's static engineering audit is complete; the remaining items are the operator's launch gate, not a separate chunk.

## Known Issues
None blocking the engineering work.
- **OnboardingPage's inverse-redirect race** (Chunk 05): on hard-reload, freshly-onboarded sessions sometimes stay on `/onboarding`. Workaround: in-app sidebar click.
- **Demo seed user can't sign in via the form** (Chunk 02): no `auth.identities` row. Use fresh sign-up for browser verification.
- **Multi-step cycle prevention is limited** (Chunk 07): only `parent_id <> child_id` is enforced.
- **Bundle size warning** (Chunk 09+): production JS ~1.31 MB (377 kB gzipped) — Vite advises code-splitting. Recharts is the bulk. Route-level `React.lazy` on the chart-heavy routes is a follow-up move, deferred from chunk 13 (no application-code changes this chunk).
- **`ANTHROPIC_MODEL` must be verified before deploy** (Chunk 11): pinned to `claude-sonnet-4-6` in `_shared/anthropic-config.ts`. Confirm the exact identifier against current Anthropic docs before the operator hits "deploy" — the value is centralized there precisely so a bump is one line.
- **AI success path + AI_TIMEOUT were not live-tested** (Chunk 11): no real `ANTHROPIC_API_KEY` was available in the engineering environment. Operator's first-deploy verification (chunk-13 runbook) exercises the success path with the production key.
- **Sidebar nav contains unimplemented routes** (pre-Chunk 12 carryover): `Pillars`, `Cadence`, `Settings` items in `NAV_ITEMS` route to paths that don't exist in `App.tsx` and therefore 404. A future chunk should either build them or trim the nav list. Chunks 12 and 13 did NOT touch this — out of scope.
- **CI builds with placeholder VITE_* env vars** (Chunk 13): the build step in `.github/workflows/ci.yml` uses fake Supabase URL + anon key so the Zod env validator in `env.ts` passes. The CI bundle is verifying compilation, not deployability against a real backend. If a runtime config issue slips past CI because of this, surface in the chunk-14 audit and consider build-time env validation.

## Decisions Made
See `/context/decisions.md`.

## Next Steps
1. **Operator step (combined chunk-13 + chunk-14 launch gate):** follow `/context/13-deployment-runbook.md` to deploy production, then return to `/context/14-pre-launch-audit.md` and fill in the OPERATOR_RUN_PENDING blocks (production SQL queries, cross-user RLS, live rate-limit verification with a real `ANTHROPIC_API_KEY`, smoke test, manual E2E, performance latency measurements). When all blocks return clean, flip the audit doc's "Launch ready" line to **YES** and the root README's status to "launched."
2. **Post-launch:** address the Known Issues catalogued in the audit's Known Issues section in priority order (bundle/recharts code-splitting; OnboardingPage redirect race; trim or build unimplemented nav routes; introduce a test runner).

## Notes for Next Agent
- **All 15 chunks have shipped.** Chunks 00-13 build + polish + deploy the product; Chunk 14 audits it. The audit is at `/context/14-pre-launch-audit.md` — every code-readable item is verified; live items are flagged OPERATOR_RUN_PENDING.
- **The audit doc is the canonical sign-off artifact.** Future agents should reference it instead of re-deriving "is RLS enabled / is `any` clean / is the envelope consistent" from the codebase. If a future change invalidates a PASS, update the audit entry in the same change.
- **Deployment is wired in config but NOT yet executed.** All the configuration artifacts ship in this repo (`frontend/vercel.json`, `backend/scripts/deploy.sh`, `scripts/smoke-test.sh`, `.github/workflows/ci.yml`, `/context/13-deployment-runbook.md`). The actual Vercel project + Supabase hosted project + first deploy + smoke run are operator steps.
- **The REST exception for `anthropic-proxy` is ratified.** Audit Item 13 documents the discriminated-dispatch pattern as the sanctioned variance. A new AI task adds a union case + a handler case to `_shared/task-schemas.ts` + `_shared/task-handlers.ts` — NOT a new Edge Function.
- **The PII boundary is now canonical** (chunk-14 decisions). PII = emails + bodies/titles/notes. Non-PII = UUIDs / counts / status enum / error codes / sizes. Any future feature must respect it. The audit checks all logger payloads, `ai_request_log` columns, and Edge Function error responses against this rule.
- **The only `security definer` Postgres function is `handle_new_user`** (auth trigger; standard Supabase pattern; `set search_path = public`). Any new `security definer` requires a new decisions entry.
- **The only `eslint-disable-next-line no-console` is in `frontend/src/constants/env.ts:16`** for the bootstrap exception. Any new exception requires a new decisions entry.
- **Migrations are forward-only.** No "down" migration. If a bad one ships, author a fix-up migration. For data-loss situations, use Supabase's point-in-time recovery. Always test with `supabase db reset` locally first.
- **Smoke test (`bash scripts/smoke-test.sh <front> <supa> <key>`) is the post-deploy gate.** 5 checks, no mutations, no PII, exits non-zero on failure. Run after every deploy — production OR preview.
- **CI runs typecheck + lint + build only** with placeholder env vars; it does NOT deploy and does NOT verify deployability. CI is for catching breakage in PRs before Vercel tries to deploy them.
- **All chunk-12 invariants hold** (shared feedback primitives, route error boundary, focus-on-route-change, toast wrapper, network-error helper, mobile hamburger, color-plus-label everywhere). Don't regress them when adding new pages.
- **No backend or application code changes were made in Chunks 13 or 14.** Pure config + audit. Build / typecheck / lint outputs unchanged.
